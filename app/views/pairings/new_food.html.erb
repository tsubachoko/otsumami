<h1>合わせ方の投稿</h1>

<h2>おつまみを選択してください</h2>

<div class="row">
  <ul>
    <% @foods.each do |food| %>
      <div class="col-md-3 col-sm-6">
        <li class="item food">
          <div class="item-title col-sm-12">
            <%= link_to food.title, new_pairing_path(food_id: food.id, drink_id: params[:drink_id]) %>
          </div>
          <div class="col-sm-12"><%= food.content %></div>
        </li>
      </div>
    <% end %>
  </ul>
</div>
